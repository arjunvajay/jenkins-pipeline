version: '3'
services:
    app:
      image: "maven-project:$BUILD_TAG" #export BUILD_TAG=1,2,3,... for testing
      build:
         context: . #current directoty
         dockerfile: Dockerfile-java

